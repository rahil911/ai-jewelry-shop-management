FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Create logs directory
RUN mkdir -p logs

# Copy source code
COPY . .

# Expose port
EXPOSE 3001

# Start the development server
CMD ["npm", "run", "dev"]