FROM node:18-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci --only=development

# Create logs directory
RUN mkdir -p logs

# Copy source code
COPY . .

# Expose port
EXPOSE 3009

# Start the development server
CMD ["npm", "run", "dev"]
